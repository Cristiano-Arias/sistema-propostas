# Dependências do Sistema de Gestão de Propostas
# Python 3.8+

# Framework Web (MANTIDAS)
Flask==2.3.3
Flask-CORS==4.0.0

# Autenticação e Segurança (MANTIDAS)
PyJWT==2.8.0
bcrypt==4.0.1
python-dotenv==1.0.0

# Banco de Dados (MANTIDAS)
SQLAlchemy==2.0.21

# Extensões Flask para melhor organização (NOVAS - NÃO QUEBRAM NADA)
Flask-SQLAlchemy==3.0.5      # Integração Flask + SQLAlchemy
Flask-JWT-Extended==4.5.2    # JWT mais robusto (compatível com PyJWT)
Flask-Bcrypt==1.0.1          # Integração Flask + Bcrypt
Flask-Limiter==3.5.0         # Rate limiting (segurança)
Flask-Migrate==4.0.5         # Migrações de banco

# Utilitários (MANTIDAS)
python-dateutil==2.8.2
pytz==2023.3

# Geração de PDF (MANTIDAS)
reportlab==4.0.4
Pillow==10.0.1

# Servidor de Produção (MANTIDAS)
gunicorn==21.2.0

# Upload de Arquivos (MANTIDAS)
Werkzeug==2.3.7

# Validação de dados (NOVA - Melhor validação)
email-validator==2.1.0

# Cache para performance (NOVA - Opcional)
Flask-Caching==2.1.0
redis==5.0.1  # Opcional, para cache em produção